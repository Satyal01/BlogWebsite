<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="	https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
    <%- include("./partials/navbar") %>
    <h1 class="text-center mt-4">Blogs</h1>
    <div class="container ">
        <div class="row row-col-4">

            <% data.blogs.map((blog)=>{ %>
                <div class="col">

                    <div class="card mt-2 p-2 col " style="width: 15rem;">
                        <img src="http://localhost:8000/<%=blog.coverImageUrl %> " class="card-img-top  rounded" alt="...">
                        <div class="card-body">
                            <% const href = "/blog/"+blog._id %>
                            <h5 class="card-title"><a href="<%=href %>" class="link-offset-2 link-offset-3-hover link-underline link-underline-opacity-0 link-underline-opacity-75-hover text-dark">  <%= blog.title %> </a></h5>
                            <p class="card-text"><%= blog.content.substr(0,10) %>....Read more</p>
                        </div>
                    </div>
                </div>
                    <%  }) %>
        </div>
    </div>
 
    <%- include("./partials/footer") %>
</body>
</html>